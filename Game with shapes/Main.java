#include<iostream>
using namespace std;
int main()
{
 int r,s;
  
  cin>>r;
  cin>>s;
  
  
  if(s>=(r+r))
 cout<<"Circle can be inside a square";
  else
   cout<<"Circle cannot be inside a square";
}